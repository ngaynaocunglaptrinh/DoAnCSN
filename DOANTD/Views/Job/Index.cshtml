@*@model IEnumerable<DOANTD.Entities.Job>

@{
    ViewBag.Title = "Danh Sách Công Việc";
}

<h2>Danh Sách Công Việc</h2>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">
        @TempData["SuccessMessage"]
    </div>
}

<table class="table">
    <thead>
        <tr>
            <th>Tiêu Đề</th>
            <th>Công Ty</th>
            <th>Địa Điểm</th>
            <th>Ngày Đăng</th>
            <th>Thao Tác</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var job in Model) // Sử dụng Model là một danh sách
        {
            <tr>
                <td>@job.Title</td>
                <td>@job.Company</td>
                <td>@job.Location</td>
                <td>@job.DatePosted.ToShortDateString()</td>
                <td>
                    @Html.ActionLink("Chỉnh Sửa", "Edit", new { id = job.Id }) |
                    @Html.ActionLink("Xóa", "Delete", new { id = job.Id })
                </td>
            </tr>
        }
    </tbody>
</table>

<a href="@Url.Action("Create")" class="btn btn-primary">Tạo Tin Mới</a>*@